<!--14.1.4 step TWO set up the html
    You MUST have a main layout located at (and named) views/layouts/main.handlebars
    All other template files will go directly in the views folder -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Just Tech News</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">Just Tech News</a>
            </h1>
            {{!-- 14.2.3 step ONE add a navigation bar --}}
            <nav>
                {{!-- 14.2.6 step THREE add a logout button --}}
                {{!-- 14.3.7 step ONE don't show the login link to users who are already logged in - 
                    and don't show the logout button to users who are already logged out --}}
                {{#if loggedIn}}
                {{!-- 14.5.3 step FIVE update the nav element to include a login link --}}
                <a href="/dashboard">dashboard</a>
                <button id="logout" class="btn-no-style">logout</button>
                {{else}}
                <a href="/login">login</a>
                {{/if}}
            </nav>
        </header>
        <main>
                {{!-- This is Handlebars.js syntax for data that will be plugged in later. 
                Three curly brackets will render the data as HTML--}}
                {{{ body }}}
        </main>
        <footer>
        Thanks for visiting!
        </footer>
    </div>
    {{!-- 14.2.6 step FOUR connect the logout.js file --}}
    {{!-- 14.3.7 step TWO wrapp the script element in an if helper so logout.js wont throw an error --}}
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>